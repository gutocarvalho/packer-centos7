{
  "builders": [
    {
      "boot_command": [
        "<tab> text ks=http://{{ .HTTPIP }}:{{ .HTTPPort }}/ks.cfg<enter>"
      ],
      "guest_os_type": "RedHat_64",
      "headless": "false",
      "http_directory": ".",
      "iso_checksum_type": "md5",
      "iso_checksum": "111379a06402e1e445c6aeee9401d031",
      "iso_url": "http://centos.ufms.br/7/isos/x86_64/CentOS-7-x86_64-NetInstall-1503.iso",
      "shutdown_command": "echo 'vagrant' | sudo -S /sbin/halt -h -p",
      "ssh_password": "vagrant",
      "ssh_username": "vagrant",
      "ssh_wait_timeout": "50000s",
      "type": "virtualbox-iso",
      "vboxmanage": [
        [
          "modifyvm",
          "{{.Name}}",
          "--memory",
          "1024"
        ],
        [
          "modifyvm",
          "{{.Name}}",
          "--cpus",
          "4"
        ]
      ],
      "vm_name": "centos7.1"
    }
  ],
  "post-processors": [
    {
      "output": "centos7x64.box",
      "type": "vagrant"
    }
  ],
  "provisioners": [
    {
      "execute_command": "echo 'vagrant' | {{.Vars}} sudo -S -E bash '{{.Path}}'",
      "scripts": [
        "vagrant.sh",
        "virtualbox.sh",
        "sysadmin.sh",
        "cleanup.sh"
      ],
      "type": "shell"
    }
  ]
}
